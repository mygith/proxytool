# proxytool 项目约定

## 语言

- 交互、代码注释、错误信息、提交说明一律使用中文

## 工程原则

- 依赖可随时升级到最新版本，不做旧版兼容
- 允许大改重构，不考虑向后兼容（状态格式、CLI 输出、日志文件均可随意变更）
- **以可读性和性能为首要目标**：
  - 优先清晰的结构与单一数据真源，避免冗余状态（运行态唯一真源为 store 的 `running` 表，其余字段一律派生，不落盘）
  - 命名直白、函数短小、能删的代码坚决删
